# 行動モデル設計書（段階3向け v0.1）

> Claude Code へ：仕様.md の 7章・8章・段階3 を、この文書が詳しく定める。
> 数値はすべて【仮】。Inspector（または1つの設定ファイル）で調整できるようにすること。
> 「アリが目的地へ向かう」「行列を作る」といった全体を見た処理は書かない。
> すべて「その個体が、その場で感じられるもの」だけで決める。

---

## 0. 設計の原則

1. **局所知覚**：アリが使える情報は、自分の内部状態と、触角の先（体の前方2点）で感じる匂い・接触だけ
2. **創発**：行列、分業、餌の分配は、個体の単純な規則の積み重ねから勝手に生まれる
3. **1本の数値の流れ**：餌 → 社会胃 → 巣の空腹 → 採餌の刺激 → 出発 → 道しるべ → 行列 → 餌。ここが閉じていること
4. **気持ちは内部状態の翻訳**：感情の変数は持たない。8章の文は内部状態から選ぶ

## 1. 時間の刻み

| 処理 | 間隔（ゲーム内時間） | 備考 |
|---|---|---|
| 移動・脚アニメ | 毎フレーム | 今の Ant.cs と同じ |
| 知覚と進路の修正 | 0.1秒【仮】 | 触角の位置の匂いを読む |
| 仕事の再判断 | 5秒【仮】 | 反応閾値の判定 |
| フェロモンの拡散 | 0.5秒【仮】 | 3章。1フレームの上限回数あり |
| 社会胃の消費 | 1秒【仮】 | 4章 |

×100 でも破綻しないように：
- **蒸発**は経過時間から解析的に計算する（`c *= exp(-λ·dt)`）。刻み幅に依存しない
- **拡散**は1フレームあたりの実行回数に上限【仮：8回】を置く。超えた分は捨てる（×100では拡散が少し弱くなるが許容）
- アリの知覚・判断は「次の判断時刻」を個体ごとに持ち、追い越した分はまとめて1回で済ませる

## 2. アリの内部状態（段階3で持つもの）

| 変数 | 範囲 | 意味 |
|---|---|---|
| `crop` | 0〜1 | 社会胃の中身。1で満腹 |
| `hunger` | 0〜1 | `1 - crop` として求める（別に持たない） |
| `ageDays` | 日 | 日齢（段階2の仮値を実際に進める） |
| `task` | 列挙 | 現在の仕事（3章の表） |
| `theta[task]` | 0〜1 | 仕事ごとの閾値（腰の重さ）。5章 |
| `carrying` | 列挙 | 運搬物。段階3では 無し / 餌 |
| `alarm` | 0〜1 | 興奮度。警報フェロモンを感じると上がり、時間で下がる（段階3では表示のみ、行動には未使用でもよい） |
| `timeOutside` | 秒 | 巣の外に出てからの経過時間 |
| `lastTrailC` | 濃度 | 直前に感じた道しるべの濃さ（「途切れた」判定用） |

- 個体差は `theta` の初期値と体の大きさ（既存）だけ。名前・能力値は作らない

## 3. フェロモンと匂いの場

土と同じ 200×250 のグリッドに、`float` の層を重ねる。

**どのマスが濃度を持つか**：空気・空洞のマスだけ。**土・石のマスは常に 0 で、拡散も通さない**。
土の中を匂いが回り込むと、壁越しに行列ができてしまうため。

| 層 | 誰が出す | 蒸発 | 拡散 | 用途 |
|---|---|---|---|---|
| 道しるべ `trail` | 餌を持って帰るアリ | 半減期 60秒【仮】 | 弱い（毎tickで隣4マスへ合計5%【仮】） | 採餌に出るアリがたどる |
| 警報 `alarm` | 危険を感じたアリ（段階6で本格化） | 半減期 10秒【仮】 | 強い（20%【仮】） | 周囲を興奮させる |
| 巣の匂い `nest` | 巣の空洞そのもの | しない | 計算で作る | 帰巣の手がかり |

### 巣の匂い（nest）の作り方
- 空洞マス（巣の内部）を発生源として、そこからの距離で減衰する値を**掘削で形が変わったときだけ**再計算する（BFS。段階4で頻繁になるが、変わったときだけなら軽い）
- 地表では入口から離れるほど薄くなる。**地表だけ**距離減衰を強め、入口から約15cm【仮】でほぼゼロになる
- 土の中は伝わらない（空気・空洞のみ伝播）
- **空洞の中にも勾配をつける**：入口から遠い（奥の）空洞ほど濃く、`nestEntranceValue`【仮：0.8】から
  `nestDeepValue`【仮：1.0】まで上がる。全部を同じ値にすると巣の中が真っ平らになり、
  `RestInNest` の「巣の匂いの濃い場所へ向かう」が働かないため。
  この勾配は段階6の「冬は巣の深い場所に集まる」にもそのまま使える
- 値の決め方：入口（空洞と空気の境目）からの距離でBFSし、
  空洞は 0.8→1.0 へ上げる、空気は 0.8 から指数で減らす（15cm でほぼゼロ）。
  したがって `nestInside`【仮：0.8】は「空洞に入った」とそのまま一致する

### 道しるべを置く規則
- **餌を持って帰る（`task == ReturnWithFood`）ときだけ**置く。行きには置かない
- 1マス通過ごとに `depositTrail`【仮：1.0】を加算。上限【仮：10】
- 置く量は餌の質に比例させる口を用意する（段階3では餌1種類なので係数1）

### 触角による感知
- 感知点は体の前方、中心から `sensorForward`【仮：0.6単位（体長の60%）】、左右に `sensorSpread`【仮：±35°】の2点
- 各点の値はそのマスの濃度（隣接補間はしない。Pointで十分）
- グリッド外・土の中は 0

## 4. 社会胃と栄養交換

### 消費
- 1秒【仮】ごとに `crop -= metabolism`。**基準：満腹から空腹まで 2日**【仮】で割る
- 移動中は消費 1.5倍【仮】、巣で静止中は 1.0倍
- `crop == 0` が 1日【仮】続いたら餓死（死体処理は段階6。段階3では「消える」でよい）

### 餌を食べる
- 餌（5章）に接触したアリは、その場で `crop` を 1 まで満たす（餌の残量から差し引く）
- そのあと `carrying = 餌` にして持ち帰る。**運ぶ分は社会胃とは別枝で、巣に着いたら床に置かず、出会った仲間に配る元手として社会胃に上乗せする**（実装上は `carryLoad`【仮：1.0】を持つ）
  - 目的：口移しで配る絵を成立させつつ、床に餌の山を作らない（餌置き場は段階5）

### 口移し（栄養交換）
- 2匹の距離が `contactRange`【仮：0.8単位】以内で、両方とも巣の中（または入口付近）にいるとき判定
- `giver.crop + giver.carryLoad` と `taker.crop` の差が `shareThreshold`【仮：0.2】以上なら、**差の半分**を 2秒【仮】かけて流す（`carryLoad` を先に使い切り、次に `crop`）
- 交換中は両者とも停止し、触角を震わせる（AntView に `Antennate()` を用意。演出ではなく状態の表示）
- **ねだる**：`hunger > begThreshold`【仮：0.6】のアリは、巣の中で「最も近い他個体」へ向かって歩く（相手の中身は知らない。近づいて触れて、差があれば流れる）

これで、外から帰った1匹の餌が、順番に薄まりながらコロニー全体へ広がる。

## 5. 餌

- 段階3は **糖分（砂糖水・アブラムシの甘露に相当）1種類**。虫の死骸などは段階6・7
- 地表にランダムな位置・間隔【仮：平均1日に1個、残量3〜8匹分】で出現する。**普通モード基準**。穏やかモードでは多め、天変地異では不安定（段階6で調整）
- アリ1匹が食べると残量 −1。0で消える
- 表示：小さな滴。残量に応じて大きさが変わる

## 6. 仕事（反応閾値モデル）

### 段階3の仕事
| task | どこで | やること |
|---|---|---|
| `RestInNest` | 巣の中 | 巣の匂いの濃い場所へ向かい、ゆっくりうろつく。ねだりもここで起きる |
| `Explore` | 地表 | 入口から出て、道しるべがあればたどり、なければ相関ランダムウォーク |
| `ReturnWithFood` | 地表→巣 | 巣の匂いの勾配を登って帰る。道しるべを置く |
| `ReturnEmpty` | 地表→巣 | 空手で帰る。道しるべは置かない |

（段階4で `Dig`、段階5で `NurseBrood` `TendQueen` を同じ枠組みに追加する）

### 刺激 S（コロニーの状態から作る。個体は「巣の中で感じられるもの」として受け取る）
- **採餌刺激** `S_forage = 巣にいるアリの hunger の平均`【0〜1】＋ `入口付近の trail 濃度 / trailMax × 0.5`【仮】
  - 前半：巣が空腹なら出る。後半：行列ができていれば釣られて出る
  - 個体は巣の中でこの値を「感じる」。厳密には接触した仲間の空腹度の移動平均などにもできるが、段階3ではコロニー平均を直接使ってよい（後で局所化しやすいよう、`S` の計算は1関数にまとめる）

### 反応確率
- 判断時刻ごとに `P = S² / (S² + θ²)` で `RestInNest → Explore` に移るか判定
- θ（`theta[Explore]`）の初期値は日齢で決める：`θ0 = clamp(1.0 - ageDays / matureDays, 0.15, 0.95)`【仮：matureDays = 20】
  - 若いほど θ が高い＝外へ出にくい。これが齢間分業になる
- **学習**：Explore を実行するたび `θ -= learn`【仮：0.05】、判断時刻に Explore を選ばなかったら `θ += forget`【仮：0.01】。下限 0.05、上限 0.95
  - これで「よく外へ出る個体」が固定されていく（能力値なしの個性）

### Explore の中身
1. 入口へ向かう（巣の匂いの勾配を**下る**）。入口を出たら `timeOutside = 0`
2. 各知覚tick：
   - 左右の `trail` を読み、`cL, cR` とする。`c = max(cL, cR)`
   - **たどる確率** `pFollow = c² / (kTrail² + c²)`【仮：kTrail = 2.0】。たどるなら、濃い側へ `turnGain × (cL−cR)/(cL+cR)`【仮：turnGain = 40°/秒】だけ曲がる
   - たどらない（または c≈0）なら相関ランダムウォーク：向きにガウス雑音【仮：σ = 25°/秒】。ただし巣の匂いが `nestFar`【仮：0.05】未満になったら、雑音に「巣の匂いが濃くなる向き」への弱い偏り【仮：10°/秒】を足す（遠出しすぎない）
   - 感知範囲【仮：1.5単位】に餌があれば真っすぐ寄って食べる → `ReturnWithFood`
3. `timeOutside > giveUpTime`【仮：20分】なら `ReturnEmpty`

### Return の中身
- 巣の匂い `nest` の勾配を左右比較で**登る**（Exploreの trail と同じ触角比較。値の向きが逆なだけ）
- `ReturnWithFood` の間だけ道しるべを置く
- 巣の内部（`nest` が `nestInside`【仮：0.8】以上）に入ったら `RestInNest` に戻り、`carryLoad` を配り始める

### 道しるべの「途切れ」
- 前tickで `pFollow > 0.5` だったのに今tickで `c < kTrail × 0.3` になったら「途切れ」フラグを 3秒【仮】立てる。行動は：その場で小さく円を描く（角速度大の雑音）— 実物の探索行動に近い。8章の「匂いが途切れた」の表示条件にも使う

## 7. 8章「気持ち」の判定順（上から最初に該当したもの）

| 順 | 条件 | 文（AntTexts） |
|---|---|---|
| 1 | `alarm > 0.5` | 何かがおかしい。落ち着かない |
| 2 | `hunger > 0.8` | お腹がすいた。誰かに分けてもらいたい |
| 3 | 途切れフラグ | 匂いが途切れた。引き返したい |
| 4 | `task == ReturnWithFood` | これを巣へ持ち帰る |
| 5 | `task == CarrySoilOut` | 土を外へ運ぶ（**段階4で追加**） |
| 6 | 地表で `nest < nestFar` | 巣の匂いが薄い。戻りたい |
| 7 | `task == Dig` | ここを広げる（**段階4で追加**） |
| 8 | `task == Explore` かつ `pFollow > 0.5` | 匂いをたどっている（**追加**） |
| 9 | `task == Explore` | 餌を探している（**追加**） |
| 10 | 栄養交換中 | 分け合っている（**追加**） |
| 11 | それ以外 | 今は穏やかだ |

- 「寒い」は段階6で追加
- 「性質」欄の内勤⇔外勤ゲージは `1 - theta[Explore]` を表示する（学習で動くのが見える）

## 8. デバッグ表示（必須）

- **F1**【仮】で道しるべ層を半透明の色で重ねて表示、**F2** で巣の匂い層。製品版で使うかは後で決めるが、開発中はこれがないと行動の正しさを判断できない
- 選択中のアリの触角感知点2つを小さな点で表示する切替（**F3**）

## 9. 性能の約束

- フェロモン層は `float[]` の一次元配列。拡散はダブルバッファで書く
- アリ同士の距離判定（口移し・ねだり）は、巣の中のアリだけを対象に、簡易グリッド（マス 2単位【仮】）で近傍だけ見る。総当たりにしない
- 200匹・×100 で Web版が 30fps を切らないことを段階3の終わりに一度測る

### 測定結果（段階3の終わり）

WebGL ビルド（Gzip、Development Build オフ、1280×720）をブラウザで動かして計測。
アリの数は Shift＋7 で増やした。

| 条件 | fps |
|---|---|
| 50匹・×100 | 60（上限に張り付き） |
| 400匹・×100 | 60（上限に張り付き） |
| 1000匹・×100 | 約45 |
| すべての匹数・×1（等速） | 60（上限に張り付き） |

ブラウザの fps は画面の更新頻度（60Hz）で頭打ちになるため、
60 に張り付いている条件は「まだ余裕がある」と読む。

約束していた 200匹・×100 は 60fps に張り付いており、**基準（30fps）は満たしている**。
その5倍の1000匹・×100 でも45fps を保っているので、当面の性能の心配はない。
改善（拡散の間引き、知覚tickのさらなる分散、描画のバッチング）は必要になってから行う。

## 10. 段階3の実装順（3回に分けて確認）

### 3a：場（フィールド）と表示
- フェロモン層3種、蒸発・拡散、巣の匂いのBFS、デバッグ表示（F1〜F3）
- 餌の出現と表示
- **確認**：F1で餌を置いた地表の匂いが広がって消えるのが見える。F2で巣から入口、地表へ匂いが薄れていく

### 3b：採餌の往復（1匹→50匹、仕事はまだ全員 Explore 固定）
- 触角感知、相関ランダムウォーク、餌の発見、帰巣、道しるべ設置、たどる
- **確認**：餌に対して自然に行列ができ、餌が尽きると数分で消える。餌を離れた場所に置き直すと、行列も移る

### 3c：社会胃・栄養交換・反応閾値
- 消費、口移し、ねだり、`S_forage`、θの初期値と学習、`RestInNest ⇄ Explore`
- 情報パネルの「気持ち」を7章の順に差し替え、ゲージを `1 - θ`に
- **確認**：最初は数匹だけ外へ出て、巣が空腹になるほど出る数が増える。帰ってきた1匹の餌が巣の中で順に配られる。日数が経つと「よく外へ出る個体」が固定されてくる（同じ個体を追跡して確かめる）

## 11. 調整値の一覧（Inspector に出すもの）

蒸発半減期(trail/alarm)、拡散率、depositTrail、trailMax、sensorForward、sensorSpread、kTrail、turnGain、探索の雑音σ、nestFar、nestInside、giveUpTime、metabolism（満腹→空腹の日数）、移動時の消費倍率、餓死までの日数、contactRange、shareThreshold、交換時間、begThreshold、matureDays、learn、forget、θの上下限、餌の出現間隔と残量、感知範囲、判断間隔、拡散の1フレーム上限回数

---

## 12. 掘削（段階4向け v0.1）

> 段階4の規則。仕様.md の 7-5「巣づくり」と段階4を、この章が詳しく定める。
> 数値はすべて【仮】で、AntSettings に出す。

### 12-1. 掘る動機（コロニーの状態から作る）

採餌が「巣の空腹」で決まるのと同じく、掘削は**混雑**で決める。

```
混雑度 = 巣の中のアリの数 ÷ 巣の空洞マス数
S_dig  = clamp01((混雑度 − comfortableDensity) / crowdRange)
```

狭くなるほど掘り、広がれば止まる。段階5で個体数が増えれば、巣も自然に広がる。
`S_forage` と同じく Colony が1か所で計算する。

### 12-2. 仕事の選び方（反応閾値を仕事ごとに持つ）

- 閾値は仕事ごとに持つ：`theta[Explore]`、`theta[Dig]`
- 判断時刻（5秒【仮】）ごとに、Explore と Dig のそれぞれで `P = S²/(S²+θ²)` を計算し、
  **順番による偏りが出ないよう、判定する順をランダムにして**、最初に当たった仕事へ移る
- 選んだ仕事の θ は `learn` だけ下がり、**選ばなかった仕事の θ は `forget` だけ上がる**
- `theta[Dig]` の初期値は**日齢に依存しない一定値**【仮：0.5】。
  掘削は内勤と外勤の中間であり、日齢で決める根拠がないため

### 12-3. 掘る場所の決め方（局所知覚だけ）

Dig のアリは**目的地を持たない**。奥へ向かうこともしない。
いつもどおり固体に接した面に沿って歩き、**壁沿いを回りながら**掘る場所に出会う。

毎知覚tick、自分のいるマスに隣接する土のマスがあれば、その中から**掘る候補を1つ選ぶ**。

> **候補の選び方（方向を持たせる）**
>
> 1. 自分のいるマスの隣にある空洞のうち、**掘削跡の匂いがいちばん濃いもの**を見つける
> 2. その**反対側**にある土マスを候補にする。掘りたての空洞ほど跡が濃いので、
>    直前に掘られた向きへ一直線に伸びる
> 3. 反対側が土でなければ（石・空洞・空気なら）**掘らない**
> 4. 跡のある空洞が隣にひとつもないときだけ、隣接する土からランダムに1つ選ぶ。
>    これが**新しいトンネルの起点**になる
>
> 跡の濃さだけで候補を選ぶと、掘りたてのマスの周り3方向が等しく候補になり、
> 先端から等方的に広がってしまう（実測で充填率 0.42 → 0.55 に悪化した）。
> 「反対側」という条件が、局所知覚のまま方向を与える。

選んだ候補について、次の確率で掘り始める。

```
pDig = digBase + digMarkerGain × 掘削跡の匂い + digCrowdGain × 周囲の混み具合
```

| 項 | 意味 | 効果 |
|---|---|---|
| `digBase`【仮】 | どこでも少しは掘る | 新しい面が開かれる |
| 掘削跡の匂い（12-4） | 候補を決めるときに見た**跡の濃さ**（跡をたどっていないときは 0 で、この項は効かない） | **トンネルの先端ほど掘られる → 穴が伸びる** |
| 周囲の混み具合 | 半径 `digCrowdRadius`【仮 1.5cm】のアリの数 ÷ `digCrowdFull`【仮】 | **アリがたまる場所が広がる → 部屋になる** |

- 周囲の混み具合は Colony の近傍バケット（9章）で数える。総当たりにしない
- **石は掘れない**。掘れる面が見つからなければ歩き続ける（結果として石を避けた形に育つ）
- 掘るのに `digSeconds`【仮 3秒】かかり、その間は止まる
- 掘り終えたらそのマスを空洞に変え、土を1粒持って `CarrySoilOut` へ

### 12-4. 掘削跡の匂い（フェロモン層をもう1枚）

| 層 | 誰が出す | 蒸発 | 拡散 | 用途 |
|---|---|---|---|---|
| 掘削跡 `dig` | 掘ったアリ | 半減期 `digHalfLife`【仮：30秒】 | **させない（0）** | 掘削を先端へ集中させる |

- 掘った瞬間に、**新しくできた空洞の1マスだけ**に `depositDig`【仮】を置く。**まわりには置かない**
- **拡散させない**。まわりへ広げると、どの面も同じくらい濃くなって先端が埋もれ、
  穴が伸びずに横へ広がってしまう（4a の実測でひとつの広い縦穴になった）
- 半減期を短くする（30秒【仮】）のも同じ理由で、**掘りたての場所だけが濃い**状態を保つため
- 読むときは 12-3 のとおり「候補の土マスに隣り合う空洞の最大値」を見る。
  トンネルの先端では、直前に掘られた空洞が隣にあるので値が高くなる
- 蒸発と保持の仕組みは道しるべと同じ（通れるマスだけに乗る・境界箱・ダブルバッファ）

### 12-5. 土の運び出しと塚

- `CarrySoilOut`：巣の匂いの勾配を**下って**入口へ出る
- 入口から `dumpMinDistance`【仮 5cm】以上離れたら、
  足元の列の地面のいちばん上に土を1マス足す（地表に塚ができる）
- 積むときの制限
  - **入口のマスは埋めない**
  - **餌が乗っているマスは埋めない**
  - **安息角**：積んだ列が隣の列より **2マス以上**高くなったら、低いほうの列へ1マス崩す。
    同じ列に積み上がり続けるのを防ぐ
- 置き終わったら手ぶらになり `RestInNest` へ戻る

### 12-6. 段階4の実装順（2回に分けて確認）

- **4a**：θ を仕事ごとに持つ／`S_dig`／掘削跡の匂いの層／`Dig`（掘って空洞にするところまで。土はまだ消える）
  - **確認**：巣が混むと掘り始め、広がると止まる。Shift+2 で巣の形が変わるのが見える
- **4b**：`CarrySoilOut` と塚、入口と餌を埋めない制限、安息角、長時間の観察
  - **確認**：放っておくと巣が育ち、地表に塚ができる。重くならない

### 12-7. 数値の調整の記録（段階4a）

50匹・×100 で流し、**アリが掘った分だけ**の充填率（空洞マス数 ÷ それを囲む四角の面積）で形を測った。
開始時の巣（手で置いた縦穴＋部屋、140マス・11×30）は、それ自体が充填率 0.42 の塊なので、
巣全体で測るとアリの規則の良し悪しが見えない。

| 試行 | 候補の選び方 | digBase | digMarkerGain | digCrowdGain | 掘った分の充填率 |
|---|---|---|---|---|---|
| 0 | 前方の土（跡は周囲に置き、拡散あり） | 0.02 | 0.08 | 0.08 | ―（巣全体 0.71） |
| 1 | 跡が最大の土 | 0.002 | 0.3 | 0 | 掘削が起きず（S_dig=0） |
| 2 | 跡が最大の土 | 0.002 | 0.3 | 0 | ―（巣全体 0.55／等方的に拡大） |
| 3 | **跡の反対側** | 0.002 | 0.3 | 0 | **0.22**（細いトンネル） |
| 6 | 跡の反対側 | 0.002 | 0.3 | **0.005** | 0.36 |
| 5 | 跡の反対側 | 0.002 | 0.3 | 0.02 | 0.42 |
| 4 | 跡の反対側 | 0.002 | 0.3 | 0.08 | 0.45（塊） |

分かったこと。

- **方向を持たせないと形にならない**。跡の濃さだけで候補を選ぶと、掘りたてのマスの周り3方向が
  等しく候補になり、先端から等方的に広がる（試行2）
- **混み具合の項は、少し入れるだけで掘る量そのものが大きく増える**。巣の中はどこでも
  アリが `digCrowdFull`（5匹）に達しやすく、混み具合がほぼ常に 1.0 になるため。
  基礎（0.002）と同じ桁の 0.005 でようやく釣り合う
- 50匹では掘る総量が小さく（130〜350マス）、**「トンネルと部屋」の区別がはっきり出るほど
  巣が育たない**。部屋らしい膨らみの確認は、個体数が増える段階5で改めて行う

採用した値：`digBase = 0.002` / `digMarkerGain = 0.3` / `digCrowdGain = 0.005` /
`comfortableDensity = 0.05` / `crowdRange = 0.15`（すべて【仮】）。
