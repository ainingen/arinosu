# arinosu（アリの巣シミュレータ）

Unity 6.3 LTS（Universal 2D）で制作する、クロオオアリの生態観察ゲーム。
仕様はプロジェクト直下の `仕様.md` を必ず参照すること。

## 役割分担

- コードは Claude Code が書く
- 設計判断と動作確認はユーザーが行う
- 数値の調整はユーザーが Inspector で行う。数値は public / [SerializeField] で外に出しておくこと

## 必ず守るルール

- 応答は日本語。C# のコメントも日本語で書く
- 実装に着手する前に、何をどう変更するつもりかを日本語で説明する
- 段階を分けて実装し、指示された段階だけを実装する。各段階の完了時に報告して、指示を待つ
- 変更後は必ず Unity の Console を確認し、エラーと警告を報告する
- MCP（MCP for Unity）が使えないときは、代替実装をせずに報告して止まる
  - Editor スクリプトを自作して回避しない
  - .unity ファイルを直接編集して回避しない
- 自動実行されるコードを作らない（[InitializeOnLoad] などエディタ起動時に走るもの）
- 以下のファイルは手で書き換えない
  - Assets/Scenes/*.unity
  - *.meta
  - ProjectSettings/
  - Packages/manifest.json
- ファイルの削除はユーザーに依頼する（.meta が対で存在するため）

## 既知の注意点

- MCP 経由でスクリプトを作ったら、Unity にフォーカスが当たるまでコンパイルされない。
  「Component type 'XXX' not found」が出たら、ユーザーに Unity のウィンドウをクリックしてもらう
- manage_components が型を見つけられないときは、Undo.AddComponent でアタッチする
- TextMeshPro の既定フォントは日本語を表示できない。日本語フォントアセットを使うこと

## 設計の方針

- アリは演出で動かさない。環境条件（匂い、温度、水、餌など）にだけ反応させる
- 「気持ち」の文言は1つのファイル（表）にまとめ、後から差し替えられるようにする
- Web ビルド（PLiCy）を想定し、個体数が増えても重くならない作りにする
